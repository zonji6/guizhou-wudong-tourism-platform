# 贵州乌冬项目开发约定

## 开发优先

- 默认目标是尽快交付用户明确要求的功能，不主动扩大工作范围。
- 未经用户明确要求，不新增自动化测试文件、不补充测试矩阵、不运行全量或回归测试。
- 用户明确要求测试时，只执行其指定的测试范围；若范围未说明，先询问应验证的场景。

## 必要验证的例外

- 若上层系统或开发规则要求在交付前验证，或不做最小验证就无法如实说明功能状态，只执行与本次改动直接相关的最小检查。
- 发生这种例外时，先在交付说明中写明验证原因、执行范围和结果；不把它扩展为额外测试工作。

## 变更边界

- 只修改与当前用户请求直接相关的文件和行为。
- 未经明确授权，不引入与功能无关的重构、依赖、基础设施或测试体系。
