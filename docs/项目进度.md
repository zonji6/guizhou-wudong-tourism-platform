# 贵州乌冬文旅平台项目进度

**项目目标：** 三天内完成可在本机操作、用于答辩的贵州乌冬文旅综合服务平台。

## 当前阶段

- 已完成：产品需求、技术架构、协作拓扑、本机演示说明，以及 Vue 门户/后台、微信小程序、Java v3 业务服务、Python 多 Agent 服务的演示主链集成。
- 当前可演示：USER 注册登录、同账号“我的”、商品/食/住三类下单、行程与草稿、社区、示意地图、ADMIN 独立登录与知识管理、AI 向导入口。
- 待接入：用户本机的 DeepSeek、百炼 Embedding 与 LangSmith API Key。

## 已明确的演示边界

- 核心场景是“苗族文化茶旅体验”：茶园体验、制茶/品茶、苗寨文化、餐饮或民宿承接。
- Java 是业务数据唯一写入口；Python AI 服务通过受控接口查询资源并生成待确认预约。
- 所有未核验的资源、价格、库存和图片是演示数据。
- 不包含真实支付、微信正式登录、真实库存、真机公网访问和生产部署。

## 本机配置提醒

模型和观测能力由用户在本机 `agent-service/.env` 提供配置；仓库不保存任何 API Key。未配置时，AI 页面应明确呈现降级提示，资源、订单和后台演示仍可继续。

## 当前协作状态

- 2026-09-10 已形成可运行集成分支 `codex/wudong-demo-integration`，本机入口为 Web `http://127.0.0.1:5174/`、Java `http://127.0.0.1:8080/`、AI `http://127.0.0.1:8002/`。
- 已实际执行 Web 生产构建、Java 跳过测试编译、Python 应用导入，以及五组公共 API、USER 注册登录和 ADMIN 登录的最小运行检查；未新增或运行自动化测试。
- 演示启动产生的日志、RSA 密钥、服务凭据与管理员密码仅保存在 `.demo-logs/`、`.demo-secrets/`，均已排除提交。
- 当前模拟项：目录价格与容量、地图坐标、部分商家和内容数据。页面需继续显示“演示/示意”标识。
- 已知未完成：携带 `sourceThreadId` 的直接下单正向授权、跨连接/跨进程 AI 取消、完整 Redis Saver 围栏；不影响普通页面直接下单与单连接演示，生产化前必须补齐。
- 下个可执行动作：在 `agent-service/.env` 配置 DeepSeek 后演示完整 AI 对话；使用微信开发者工具打开 `miniprogram/` 完成开发者工具验收。
- 2026-09-11 修复集成工作树缺少本机派生图的问题：已从用户素材生成 Web 16 张、小程序 8 张图片，并补充可重复执行的素材脚本；派生图继续由 Git 忽略，不上传公开仓库。
- 2026-09-11 已重启本机 Web（5174）、Java（8080）和 AI（8002）服务；Flyway V6 已由运行日志确认加载。Web 公开目录、匿名向导会话和一轮两日慢游卡已在浏览器最小流程中实际运行。
- 向导本轮修复：匿名 cookie 同步回写 threadId；前端先请求会话状态后再生成；中文关键词检索采用连续双字片段；Java 发布读取兼容 MySQL `LocalDateTime`；地点内部接口按实际列表契约解析；候选持久化移除未定义字段。四篇带来源与限制的乌东演示资料已通过后台发布并参与检索。
- DeepSeek 仍未配置：本机使用明确标记“未调用模型”的资料编排回退，只串联已发布资料和公开演示地点，不生成地方事实。配置 `agent-service/.env` 后才会进入 DeepSeek 生成链路。
- 小程序“乌东向导”已接入与 Web 一致的 V3 WebSocket 会话、会话状态同步、资料编排行程卡、资料引用和实时阶段提示；尚未在微信开发者工具完成真实连接验收。
- 运营后台“目录概览”已改为真实目录管理台：可选择商品、菜品、住宿、房型、地点等记录，编辑既有字段，并通过 Java 已有的版本校验接口保存、发布、下架或归档；未在本轮提交中新增或改写实际目录数据。
- Web 账号向导的“保存个人行程”已调用 Java 的候选采用接口；匿名向导仍只展示建议，登录后须重新由账号向导生成并由用户主动保存。
- “我的／已保存行程”已提供手动调整标题、日期、人数、每日主题、停留顺序与说明的编辑器，保存沿用 Java 条件版本更新；发生冲突时页面内容不会自动覆盖最新服务端版本。
- 小程序“订单、草稿与行程”页已对齐 Web 的已保存行程编辑能力，可在单列编辑器中调整每日主题、停留顺序与说明并主动保存；尚未在微信开发者工具完成真实保存链路验收。
- 小程序账号向导生成行程后可主动保存新行程或采用基于既有行程的更新候选；匿名预览不触发个人数据写入。
- Web 与小程序账号向导均支持将餐食或住宿候选保存为草稿；草稿仍须在“我的”中补齐联系人、核价并明确提交，未被采用的候选不会写入业务库。
- 运营后台目录台已补齐新建入口：商家、商品、菜品／饮品、住宿、房型和地点均可通过同一管理台创建；有归属关系的记录须选择商家或住宿主体，创建结果默认未发布，仍需运营者主动发布后才进入游客端。
- 目录分组默认仅展示前四条以保持看板紧凑，但现已支持展开全部记录；每条记录都可以进入同一编辑、发布、下架或归档流程。
- Web 餐食模块已补充店铺关键词筛选，可按店名、简介或标签缩小公开店铺范围，并同步显示匹配数量；不改变同店多菜订单规则。
- Web 社区的示意路线攻略已从单地点提交升级为可编排最多 12 个公开地点的有序节点；发布前在页面明确显示选取顺序，仍只作为非导航水彩示意。
- 小程序社区投稿已同步支持最多 12 个公开地点的有序路线编排；登录后才允许发布，编号明确表示水彩示意顺序。
- 小程序餐食模块已同步支持按店名、简介或标签筛选公开店铺，并显示匹配数量；同店多菜的选餐、核价与下单流程不变。
- 小程序“行”模块已补充自选水彩示意路线：可从地图节点或地点卡片按顺序选择最多 12 个地点、查看连线与编号并主动清空；其位置和路线仍非真实导航。
- 运营后台已补齐知识来源、知识草稿的新建与版本编辑表单：草稿可引用具体来源版本，保存不等于发布；仍由既有发布任务生成公开知识版本。
- Web 与小程序向导已增加餐食、住宿的公开目录推荐快捷入口；服务 Agent 先按完整自然语言检索，未命中时才退回相应类别的代表性公开目录检索，避免长句提问直接返回空结果。食宿草稿仍必须由用户在资源页明确选定同店菜品或房型并补齐条件后才会生成，不会替用户擅自创建草稿。
- AI 餐食推荐卡已补足“店铺名 · 菜名”的归属信息，并提示同店选餐规则；同名菜品来自不同店铺时不再无法区分。
- 运营后台订单台已增加商品／餐食／住宿分类与餐饮店铺筛选，并在餐食主单卡中展示同单菜品、份数、行金额、到店时间和人数；状态推进仍沿用 Java 原有的合法流转。
- 运营总览新增公开目录构成条形看板，按商品、餐食、住宿房型与地点的接口实际数量展示，不以模拟数据填充 GMV、客流或转化指标。
- Web 住宿模块已补充关键词与单间人数适配筛选，可从 10 家住宿、33 种房型中快速缩小范围；筛选只依据公开目录资料和演示容量，不表达实时房态。
- Web 社区已补充“寨里动态／示意路线”与标签筛选，并支持 `#/explore/community` 直达；路线摘要统一补充非真实导航说明，避免把水彩节点误读为地图服务。
- 小程序社区已同步动态／路线与标签筛选，筛空时给出清晰提示；投稿、最多 12 个公开地点的路线编排及非导航说明保持不变，待微信开发者工具验收。
- 小程序住宿模块已对齐 Web 的关键词与单间人数筛选，可依据住宿、房型、位置和公开演示容量筛选；不表达实时房态，待微信开发者工具验收。
- 小程序向导已展示完整的“理解需求—检索资料—核对资料—匹配体验—整理建议”阶段，并把公开推荐卡的查看动作接回商品、食、住、行模块；仍待微信开发者工具验收真实 WebSocket 连接。
- Web 社区首屏标签已按实际使用频次收束为 12 个，减少标签墙对文化文章与路线攻略的挤占；原文标签仍完整保留在各内容卡中。
- 已在浏览器实际巡检 Web 商品、餐食、住宿、行与社区：商品目录与资料图可展示；餐饮可进入具体店铺菜单；“金家”关键词可收束到 1 家住宿；社区路线可带入水彩示意图并呈现有序节点。该巡检未提交订单或修改业务数据。
- 向导意图路由已补充“两天／一夜／一日／安排”等行程词，修复“两天一夜茶旅”快捷入口被误分到资料解答的问题；浏览器已实际验证免登录 WebSocket 会话返回“两日慢游”行程卡与 3 条资料来源。未配置 DeepSeek 时仍明确显示“未调用模型”。
- 向导公开推荐卡已按目标类别合并重复动作：餐食推荐保留“店铺 · 菜名”区分，并只提供一个“去同店选餐”入口；同样规则适用于商品、住宿、地点与示意路线，避免多条同名操作误导用户。
- 首页手动开卷已修复为“先准备五景、再移开门帘”的覆盖式过渡：门帘离开时可直接显出山、水、寨、茶、人实拍首页，不再在动画末尾露出空白底色；已在浏览器中实际截图确认。
- 运营总览已收束目录口径：资源构成和商品／餐食／房型卡均明确展示目录总数与已发布数，不再把后台可见的未发布或归档记录称为游客端“公开内容”；继续不展示虚构 GMV 与实时客流。
- 向导服务意图已补充苗绣、蜡染、银饰等乌东手作词：这些请求会定位到商品目录而不是默认地点。浏览器已实际验证“亲子苗绣”返回公开苗绣／银饰／服饰商品及唯一“查看商品目录”入口；价格仍明确为演示价并在 Java 下单前重新核价。
- 向导目录检索在完整自然语言未精确命中时，现先保留苗绣、蜡染、银饰、银球茶、酸汤鱼等在地主题词，再退回类别代表词；推荐卡最多展示 8 条，完整目录通过一个明确入口承接。浏览器已验证“亲子苗绣”收束为 7 条苗绣相关商品，首项不再是无关泛银饰。
- 小程序商品模块已补齐与食住一致的“公开总数／筛选匹配数”提示；行模块的孤立 `wx:else` 已改为明确的“暂无路线”条件，避免模板语义错误。仅完成 JavaScript 静态检查，仍待微信开发者工具渲染验收。
- 未配置 DeepSeek 时，服务推荐卡现与行程及资料问答保持一致，明确标注“资料编排模式（未调用模型）”；浏览器已验证酸汤鱼推荐仍展示店铺归属、同店选餐规则、演示价与 Java 核价边界。配置模型后该提示自动恢复为普通公开目录说明。
- 小程序向导的公开服务推荐卡已对齐 Web：展示演示价／资料参考价、标签和演示资料提示，价格明确在提交前由 Java 重新核价；标签为空时不渲染标签区。尚无微信开发者工具会话，此项未作为真机验收完成。
- 已知未完成：小程序五模块与 AI 向导仍待微信开发者工具实机验收；AI 停机中断的旧匿名运行不会自动恢复，重新开启免登录会话可继续演示。

## 2026-09-11 内容模块集成状态

- `TASK-031` 与 `TASK-032` 已分别经永久独立验收 `PASS` 并标记 `VERIFIED`：数据制品 `6ffb3a5` 已集成为 `99381aa`，双端体验制品 `0940807` 已集成为 `7b08315`；内容控制收尾时的集成 HEAD 为 `558e21b`。
- V6 的 27 个商品、10 家住宿／33 个房型、12 家餐饮／100 个菜品饮品／14 个套餐、6 个地点、3 条示意路线及 4 篇文化文章均是静态迁移数量，未冒充数据库实测。228 张资料图已准备为 456 个本机正图／缩略图文件。
- 本轮独立验收通过 V6 静态范围与离线 Java 编译、Web 生产构建、小程序 JavaScript／WXML 静态检查和媒体映射；另已读取 5 张代表性资料图 HTTP 200。这些检查不等于新数据已加载或双端产品验收。
- 自动停止／重启 Java 被本机执行策略拒绝，旧 8080 服务仍返回 1 个商品，V6 尚未加载。用户应手动重启 `.worktrees/demo-integration/tourism-service` 的既有 Java 服务（不清数据库、不启动主目录旧代码），随后核对 Flyway V6、实际目录数量以及 Web／微信开发者工具五模块。
- 全局 `pre-deliver` 仍仅由历史 TASK-005／006／007 缺少匹配 PASS 记录而 `BLOCKED`；031／032 已不再触发该错误，不能据此宣称整体项目交付门通过。详情见 [本轮交付说明](source-analysis/2026-09-11-content-module-delivery.md)。
