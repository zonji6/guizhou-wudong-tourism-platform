# 贵州乌冬文旅平台项目进度

**项目目标：** 三天内完成可在本机操作、用于答辩的贵州乌冬文旅综合服务平台。

## 当前阶段

- 已完成：产品需求、技术架构、协作拓扑、本机演示说明，以及 Vue 门户/后台、微信小程序、Java v3 业务服务、Python 多 Agent 服务的演示主链集成。
- 当前可演示：USER 注册登录、同账号“我的”、商品/食/住三类下单、行程与草稿、社区、示意地图、ADMIN 独立登录与知识管理、AI 向导入口。
- 待接入：用户本机的 DeepSeek、百炼 Embedding 与 LangSmith API Key。

## 已明确的演示边界

- 核心场景是“苗族文化茶旅体验”：茶园体验、制茶/品茶、苗寨文化、餐饮或民宿承接。
- Java 是业务数据唯一写入口；Python AI 服务通过受控接口查询资源并生成待确认预约。
- 所有未核验的资源、价格、库存和图片是演示数据。
- 不包含真实支付、微信正式登录、真实库存、真机公网访问和生产部署。

## 本机配置提醒

模型和观测能力由用户在本机 `agent-service/.env` 提供配置；仓库不保存任何 API Key。未配置时，AI 页面应明确呈现降级提示，资源、订单和后台演示仍可继续。

## 当前协作状态

- 2026-09-10 已形成可运行集成分支 `codex/wudong-demo-integration`，本机入口为 Web `http://127.0.0.1:5174/`、Java `http://127.0.0.1:8080/`、AI `http://127.0.0.1:8002/`。
- 已实际执行 Web 生产构建、Java 跳过测试编译、Python 应用导入，以及五组公共 API、USER 注册登录和 ADMIN 登录的最小运行检查；未新增或运行自动化测试。
- 演示启动产生的日志、RSA 密钥、服务凭据与管理员密码仅保存在 `.demo-logs/`、`.demo-secrets/`，均已排除提交。
- 当前模拟项：目录价格与容量、地图坐标、部分商家和内容数据。页面需继续显示“演示/示意”标识。
- 已知未完成：携带 `sourceThreadId` 的直接下单正向授权、跨连接/跨进程 AI 取消、完整 Redis Saver 围栏；不影响普通页面直接下单与单连接演示，生产化前必须补齐。
- 下个可执行动作：在 `agent-service/.env` 配置 DeepSeek 后演示完整 AI 对话；使用微信开发者工具打开 `miniprogram/` 完成开发者工具验收。
- 2026-09-11 修复集成工作树缺少本机派生图的问题：已从用户素材生成 Web 16 张、小程序 8 张图片，并补充可重复执行的素材脚本；派生图继续由 Git 忽略，不上传公开仓库。
- 2026-09-11 已重启本机 Web（5174）、Java（8080）和 AI（8002）服务；Flyway V6 已由运行日志确认加载。Web 公开目录、匿名向导会话和一轮两日慢游卡已在浏览器最小流程中实际运行。
- 向导本轮修复：匿名 cookie 同步回写 threadId；前端先请求会话状态后再生成；中文关键词检索采用连续双字片段；Java 发布读取兼容 MySQL `LocalDateTime`；地点内部接口按实际列表契约解析；候选持久化移除未定义字段。四篇带来源与限制的乌东演示资料已通过后台发布并参与检索。
- DeepSeek 仍未配置：本机使用明确标记“未调用模型”的资料编排回退，只串联已发布资料和公开演示地点，不生成地方事实。配置 `agent-service/.env` 后才会进入 DeepSeek 生成链路。
- 小程序“乌东向导”已接入与 Web 一致的 V3 WebSocket 会话、会话状态同步、资料编排行程卡、资料引用和实时阶段提示；尚未在微信开发者工具完成真实连接验收。
- 运营后台“目录概览”已改为真实目录管理台：可选择商品、菜品、住宿、房型、地点等记录，编辑既有字段，并通过 Java 已有的版本校验接口保存、发布、下架或归档；未在本轮提交中新增或改写实际目录数据。
- Web 账号向导的“保存个人行程”已调用 Java 的候选采用接口；匿名向导仍只展示建议，登录后须重新由账号向导生成并由用户主动保存。
- 已知未完成：后台经营看板、目录编辑、发布／下架／归档与订单状态处理已接入，但尚未提供运营侧的目录新建表单；小程序五模块与 AI 向导仍待微信开发者工具实机验收；AI 停机中断的旧匿名运行不会自动恢复，重新开启免登录会话可继续演示。

## 2026-09-11 内容模块集成状态

- `TASK-031` 与 `TASK-032` 已分别经永久独立验收 `PASS` 并标记 `VERIFIED`：数据制品 `6ffb3a5` 已集成为 `99381aa`，双端体验制品 `0940807` 已集成为 `7b08315`；内容控制收尾时的集成 HEAD 为 `558e21b`。
- V6 的 27 个商品、10 家住宿／33 个房型、12 家餐饮／100 个菜品饮品／14 个套餐、6 个地点、3 条示意路线及 4 篇文化文章均是静态迁移数量，未冒充数据库实测。228 张资料图已准备为 456 个本机正图／缩略图文件。
- 本轮独立验收通过 V6 静态范围与离线 Java 编译、Web 生产构建、小程序 JavaScript／WXML 静态检查和媒体映射；另已读取 5 张代表性资料图 HTTP 200。这些检查不等于新数据已加载或双端产品验收。
- 自动停止／重启 Java 被本机执行策略拒绝，旧 8080 服务仍返回 1 个商品，V6 尚未加载。用户应手动重启 `.worktrees/demo-integration/tourism-service` 的既有 Java 服务（不清数据库、不启动主目录旧代码），随后核对 Flyway V6、实际目录数量以及 Web／微信开发者工具五模块。
- 全局 `pre-deliver` 仍仅由历史 TASK-005／006／007 缺少匹配 PASS 记录而 `BLOCKED`；031／032 已不再触发该错误，不能据此宣称整体项目交付门通过。详情见 [本轮交付说明](source-analysis/2026-09-11-content-module-delivery.md)。
