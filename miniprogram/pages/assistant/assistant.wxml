<view class="page"><view class="eyebrow">乌东向导 · AI 入口</view><view class="title">先确认身份，再把心愿交给山茶</view><view class="muted">本页只执行已冻结的 wudong-ws-auth-v1 首帧。在 assistant-card-v3 交付前，不猜测卡片、候选或保存事件。</view><view class="guide-card"><view class="leaf">叶</view><view><view class="card-title">{{account ? account.nickname + '，准备连接账号向导' : '免登录体验匿名向导'}}</view><view class="muted">{{message || (account ? 'USER 失效时不会降级匿名，也不会自动重发需求。' : '匿名预览不能读取账号资料；需要保存成果时再登录并再次确认。')}}</view></view><button class="primary" disabled="{{status === 'connecting' || status === 'ready'}}" bindtap="connect">{{status === 'ready' ? '身份通道已就绪' : status === 'connecting' ? '连接中…' : anonymousNeedsRenew ? '重新开始匿名预览' : account ? '连接账号向导' : '开始匿名预览'}}</button><button wx:if="{{!account}}" class="link" bindtap="toProfile">已有账号？去“我的”登录</button></view><view class="quick"><button data-kind="product" bindtap="toResources">先看看商品</button><button data-kind="food" bindtap="toResources">同店选餐</button><button data-kind="travel" bindtap="toResources">看示意路线</button></view></view>
