<view class="page"><view class="eyebrow">乌东向导</view><view class="title">先确认身份，再把心愿交给山茶</view><view class="muted">向导目前可以确认身份并建立连接，更多行程内容正在准备中。</view><view class="guide-card"><view class="leaf">叶</view><view><view class="card-title">{{account ? account.nickname + '，准备连接账号向导' : '免登录体验匿名向导'}}</view><view class="muted">{{message || (account ? '登录失效时不会自动切换身份，也不会自动重发需求。' : '匿名预览不能读取账号资料；需要保存成果时再登录并再次确认。')}}</view></view><button class="primary" disabled="{{status === 'connecting' || status === 'ready'}}" bindtap="connect">{{status === 'ready' ? '向导已就绪' : status === 'connecting' ? '连接中…' : anonymousNeedsRenew ? '重新开始匿名预览' : account ? '连接账号向导' : '开始匿名预览'}}</button><button wx:if="{{!account}}" class="link" bindtap="toProfile">已有账号？去“我的”登录</button></view><view class="quick"><button data-kind="product" bindtap="toResources">先看看商品</button><button data-kind="food" bindtap="toResources">同店选餐</button><button data-kind="travel" bindtap="toResources">看示意路线</button></view></view>
