const { services } = require('../../utils/demo')
Page({ data: { item: services[0], date:'2026-09-20', people:2, contact:'答辩体验官', done:false }, onLoad(options) { this.setData({ item: services.find(item => item.id === Number(options.id)) || services[0] }) }, dateChange(e) { this.setData({ date:e.detail.value }) }, peopleChange(e) { this.setData({ people:e.detail.value }) }, contactChange(e) { this.setData({ contact:e.detail.value }) }, submit() { const app = getApp(); app.globalData.orders.unshift({ id:'WD' + Date.now().toString().slice(-9), name:this.data.item.title, date:this.data.date, people:this.data.people, status:'待确认' }); this.setData({ done:true }) }, orders() { wx.navigateTo({ url:'/pages/orders/orders' }) } })
