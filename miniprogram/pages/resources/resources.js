const { services, normalizeService } = require('../../utils/demo'); const { request } = require('../../utils/api'); const categories=[{id:'all',name:'全部'},{id:'culture',name:'苗韵茶旅'},{id:'stay',name:'暖居民宿'},{id:'food',name:'寨味餐食'},{id:'travel',name:'山野出行'}]; let allServices=services
Page({data:{active:'all',services,categories,imageErrors:{}},onLoad(){request('/api/services').then(items=>{allServices=Array.isArray(items)&&items.length?items.map(normalizeService):services;this.applyFilter(this.data.active)}).catch(()=>this.applyFilter(this.data.active))},onShow(){this.getTabBar()?.setData({selected:1});const app=getApp(),requested=app.globalData.resourceCategory;app.globalData.resourceCategory=null;this.applyFilter(requested||this.data.active)},applyFilter(category){this.setData({active:category,services:category==='all'?allServices:allServices.filter(item=>item.category===category)})},filter(e){this.applyFilter(e.currentTarget.dataset.id)},toDetail(e){wx.navigateTo({url:'/pages/detail/detail?id='+encodeURIComponent(e.currentTarget.dataset.id)})},imageError(e){const id=e.currentTarget.dataset.id;this.setData({imageErrors:{...this.data.imageErrors,[id]:true}})}})
